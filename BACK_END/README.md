## API
To run server:

    npm install
    npm run dev

Make sure to copy `.env.example` to your own `.env` file and add DB information

Windows:

    copy .env.example .env
Linux/Mac:

    cp .env.example .env
